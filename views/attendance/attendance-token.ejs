<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Token</title>
    <link rel="stylesheet" href="/css/register.css" />

    <%- include('../includes/bootstrap.ejs') %>

</head>
<body>
    <form action="/attendance-token" method="POST" id="token-form">
        <div class="form-group" id="email-wrapper">
          <input type="email" class="form-control" id="email" aria-describedby="emailHelp" placeholder="Email" name="email">
        </div>
        <button type="submit" class="btn btn-warning" id="generate-token-btn">Generate Attendance Token</button>
    </form>
    <% if (notStudentEmail) { %>
        <div class="alert alert-danger" role="alert">
            Must have a GNPS student email!
        </div>
    <% } else if (isEmailInUse) { %>
        <div class="alert alert-danger" role="alert">
            Token has already been sent to this email. Check your inbox and spam!
        </div>
    <% } else if (attendanceTokenSent) { %>
        <div class="alert alert-success" role="alert">
            Token has been sent to your email! Check your inbox and spam folder. 
        </div>
    <% } %>
</body>
</html>