<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Token</title>
    <link rel="stylesheet" href="/css/attendance/attendance.css" />

    <%- include('../includes/bootstrap.ejs') %>
</head>
<body>
    <div id="attendance-btn-wrapper-right">
        <a href="/attendance">
            <button type="button" class="btn btn-success">Click to view our attendance list!
                <img src="https://img.icons8.com/ios/50/000000/circled-right.png" width="25" height="25"/>
            </button>
        </a>
    </div>
    <div id="form-wrapper">
        <form action="/attendance-token" method="POST" id="token-form">
            <div class="alert alert-info" role="alert">
                <img src="https://img.icons8.com/ios-glyphs/30/000000/github.png"/> View the (very messy and honestly bad) code for this website <a href="https://github.com/nhs-computer-science/website-chat" target="_blank">here</a>!
            </div>
            <div class="alert alert-info" role="alert">
                Want to chat live? Click <a href="/login">here</a> to log in!
            </div>
            <div class="alert alert-info" role="alert">
                Don't have an account? Click <a href="/register">here</a> to register!
            </div>
            <img src="/images/logo.png" width="60" height="60"/>
            <div class="form-group" id="token-wrapper">
              <input type="email" class="form-control" id="token" aria-describedby="emailHelp" placeholder="Email" name="email" required>
            </div>
            <% if (notStudentEmail) { %>
                <div class="alert alert-danger" role="alert">
                    Must have a GNPS student email!
                </div>
            <% } else if (isEmailInUse) { %>
                <div class="alert alert-danger" role="alert">
                    Token has already been sent to this email. Check your inbox and spam!
                </div>
            <% } else if (attendanceTokenSent) { %>
                <div class="alert alert-success" role="alert">
                    Token has been sent to your email; check your inbox and spam folder! If you did not receive the token, please wait 2 minutes before generating another. 
                </div>
            <% } %>
            <div id="submit-btn-wrapper">
                <button type="submit" class="btn btn-info">Generate Attendance Token</button>
            </div>
            <p>Have a token? Submit it <a href="/submit-attendance">here</a></p>
        </form>
    </div>
</body>
</html>
