<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/authentication.css" />

    <%- include('../includes/bootstrap.ejs') %>

    <script defer>

    </script>
</head>
<body>
    <div id="form-wrapper">
        <form action="/login" method="POST" id="login-form" onsubmit="showLoadingSpinner('loading-spinner-wrapper')">
            <div class="alert alert-info" role="alert">
                Want to submit your attendance token? Click <a href="/attendance">here!</a>
            </div>
            <div class="alert alert-info" role="alert">
                Dont't have an attendance token? Click <a href="/attendance-token">here</a> to get one!
            </div>
            <img src="/images/logo.png" width="60" height="60"/>
            <input type="email" class="form-control" id="email" aria-describedby="emailHelp" placeholder="Email" name="email" required>
            <% if (authFailed) { %>
                <p class="auth-failed">Invalid email and/or password!</p>
            <% } %>
            <input type="password" class="form-control" id="password" aria-describedby="emailHelp" placeholder="Password" name="password" required value="testA12345">
            <% if (authFailed) { %>
                <p class="auth-failed">Invalid email and/or password!</p>
            <% } %>
            <div id="submit-btn-wrapper">
                <button type="submit" class="btn btn-info" id="login-btn">Log In</button>
                <div class="spinner-border text-primary loading-spinner-wrapper" role="status">
                    <span class="sr-only">Loading...</span>
                </div>
            </div>
            <p>Don't have an account? Register <a href="/register">here!</a></p>   
        </form> 
    </div>
</body>
</html>